{
  "private": true,
  "name": "api-server",
  "version": "1.0.0",
  "scripts": {
    "dev": "json-server --watch db.json --port 4000"
  },
  "devDependencies": {
    "json-server": "0.17.4",
    "rimraf": "^6.0.1"
  }
}
